<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.wean2016.mapper.StudentMapper">

    <select id="getStudentById" parameterType="int" resultType="Student">
        SELECT * FROM student WHERE id=#{id}
    </select>

    <select id="getAllStudent" resultType="Student">
        SELECT * FROM student
    </select>

    <insert id="addStudent" parameterType="Student">
        INSERT INTO student(id, studentName, studentGender) VALUES (#{id}, #{studentName}, #{studentGender})
    </insert>

    <update id="updateStudent" parameterType="Student">
        UPDATE student SET studentName=#{studentName}, studentGender=#{studentGender} WHERE id=#{id}
    </update>

    <delete id="deleteStudent" parameterType="int">
        DELETE FROM student WHERE id=#{id}
    </delete>
</mapper>